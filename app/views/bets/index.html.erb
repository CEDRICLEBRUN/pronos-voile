<div>
  <h1 class="font-bold text-4xl">Bets#index</h1>
  <p>Find me in app/views/bets/index.html.erb</p>
  <% @categories.each do |category| %>
    <%= category.first %>
    <%= image_tag category.last %>
  <% end %>
</div>
